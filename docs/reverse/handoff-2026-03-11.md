# Disassembly Session Handoff (2026-03-11)

## What was confirmed
- `Sopwith.exe` is not a standard DOS `MZ` image at file offset `0`.
- The binary begins with valid-looking 16-bit x86 instructions and DOS interaction patterns.
- A large string/resource region exists around `0x9B80+` with gameplay/help/network text.
- A first-pass 8086 disassembly listing was produced from the candidate code segment.

## What changed
- Added repeatable extraction script: `scripts/Extract-SopwithArtifacts.ps1`.
- Generated artifact bundle under `artifacts/disasm` including:
  - `triage.json`
  - `segments/code_candidate.bin`
  - `segments/assets_candidate.bin`
  - `disassembly/code_candidate.objdump.asm`
  - `transpile/sopwith_image.asm`
- Added baseline reverse docs:
  - `docs/reverse/triage.md`
  - `docs/reverse/extraction-manifest.md`

## Open questions
- Precise loader/origin assumptions for interpreting addresses in static tools.
- Which portions of `assets_candidate.bin` correspond to sprite/tile/music tables.
- True function boundaries and update order without runtime trace correlation.

## Next 3 tasks
1. Import `segments/code_candidate.bin` in Ghidra as 16-bit raw binary and map startup routines.
2. Capture DOSBox-X runtime traces for startup and argument parsing to validate control-flow hypotheses.
3. Build initial symbol map (`input`, `update`, `render`, `collision`, `scoring`) and start behavioral-spec sections.
